// MainDialog class implementation
#include "description_dialogs.hpp"

#define rows 32
#define columns 28
#define main_y 0.0
#define main_x (1 - (1 * 0.75)) / 2
#define cell_first_x 0.05 + main_x
#define cell_first_y 0.05 + main_y
#define cell_h 0.8 / rows
#define cell_w cell_h * 0.75


class MyImageMain {
	idc = 10;								            // id of control
	type = CT_STATIC;							        // control type
	style = ST_PICTURE + ST_KEEP_ASPECT_RATIO;          // 'Text' property interpreted as img path, keep img aspect ratio
	y = 0.0 * cell_h + cell_first_y;			        // image position vertical
	x = 0.0 * cell_w + cell_first_x;	                // image position horizontal
	h = cell_h;         			                    // image height
	w = cell_w;                                         // image width
	colorText[] = {1, 1, 1, 1};					        // color for text
	colorBackground[] = {0, 1, 0, 0};                   // color for background
	font = "TahomaB";							        // font to use
	sizeEx = 0.001;							            // font size
	text = "";                                 			// displayed text, image cords
};

class MainDialog {                              
	idd = 20000;									    // id of dialog
	movingEnable = 0;								    // cannot be moved

	class Controls {
		
		class Background {
			idc = 12;					                // id of control
			type = CT_STATIC;							// control type
			style = ST_LEFT;							// text is centered
			h = safezoneH;      						// hud height
			w = safezoneW;		       					// hud width
			y = safezoneY;								// hud position vertical
			x = safezoneX;					            // hud position horizontal
			colorText[] = {1, 1, 1, 1};					// color for text 
			colorBackground[] = {0, 0, 0, 1};     		// and background
			font = "TahomaB";							// font to use
			SizeEx = 0.05;								// font size
			text = "";									// displayed text
		};
		
		class MyMessage {
			idc = 13;					                // id of control
			type = CT_STATIC;							// control type
			style = ST_CENTER;							// text is centered
			h = 1;      								// hud height
			w = 1 * 0.75;       					    // hud width
			y = main_y;									// hud position vertical
			x = main_x;					                // hud position horizontal
			colorText[] = {1, 1, 1, 1};					// color for text 
			colorBackground[] = {0.5, 0.5, 0.5, 1};     // and background
			font = "TahomaB";							// font to use
			SizeEx = 0.05;								// font size
			text = "Choose map";						// displayed text
		};

        class MyImage0: MyImageMain {
			idc = 100;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage1: MyImageMain {
			idc = 101;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage2: MyImageMain {
			idc = 102;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage3: MyImageMain {
			idc = 103;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage4: MyImageMain {
			idc = 104;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage5: MyImageMain {
			idc = 105;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage6: MyImageMain {
			idc = 106;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage7: MyImageMain {
			idc = 107;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage8: MyImageMain {
			idc = 108;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage9: MyImageMain {
			idc = 109;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage10: MyImageMain {
			idc = 110;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage11: MyImageMain {
			idc = 111;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage12: MyImageMain {
			idc = 112;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage13: MyImageMain {
			idc = 113;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage14: MyImageMain {
			idc = 114;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage15: MyImageMain {
			idc = 115;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage16: MyImageMain {
			idc = 116;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage17: MyImageMain {
			idc = 117;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage18: MyImageMain {
			idc = 118;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage19: MyImageMain {
			idc = 119;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage20: MyImageMain {
			idc = 120;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage21: MyImageMain {
			idc = 121;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage22: MyImageMain {
			idc = 122;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage23: MyImageMain {
			idc = 123;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage24: MyImageMain {
			idc = 124;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage25: MyImageMain {
			idc = 125;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage26: MyImageMain {
			idc = 126;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage27: MyImageMain {
			idc = 127;								    // id of control
			y = 0.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage28: MyImageMain {
			idc = 128;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage29: MyImageMain {
			idc = 129;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage30: MyImageMain {
			idc = 130;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage31: MyImageMain {
			idc = 131;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage32: MyImageMain {
			idc = 132;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage33: MyImageMain {
			idc = 133;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage34: MyImageMain {
			idc = 134;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage35: MyImageMain {
			idc = 135;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage36: MyImageMain {
			idc = 136;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage37: MyImageMain {
			idc = 137;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage38: MyImageMain {
			idc = 138;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage39: MyImageMain {
			idc = 139;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage40: MyImageMain {
			idc = 140;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage41: MyImageMain {
			idc = 141;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage42: MyImageMain {
			idc = 142;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage43: MyImageMain {
			idc = 143;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage44: MyImageMain {
			idc = 144;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage45: MyImageMain {
			idc = 145;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage46: MyImageMain {
			idc = 146;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage47: MyImageMain {
			idc = 147;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage48: MyImageMain {
			idc = 148;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage49: MyImageMain {
			idc = 149;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage50: MyImageMain {
			idc = 150;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage51: MyImageMain {
			idc = 151;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage52: MyImageMain {
			idc = 152;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage53: MyImageMain {
			idc = 153;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage54: MyImageMain {
			idc = 154;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage55: MyImageMain {
			idc = 155;								    // id of control
			y = 1.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage56: MyImageMain {
			idc = 156;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage57: MyImageMain {
			idc = 157;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage58: MyImageMain {
			idc = 158;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage59: MyImageMain {
			idc = 159;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage60: MyImageMain {
			idc = 160;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage61: MyImageMain {
			idc = 161;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage62: MyImageMain {
			idc = 162;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage63: MyImageMain {
			idc = 163;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage64: MyImageMain {
			idc = 164;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage65: MyImageMain {
			idc = 165;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage66: MyImageMain {
			idc = 166;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage67: MyImageMain {
			idc = 167;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage68: MyImageMain {
			idc = 168;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage69: MyImageMain {
			idc = 169;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage70: MyImageMain {
			idc = 170;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage71: MyImageMain {
			idc = 171;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage72: MyImageMain {
			idc = 172;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage73: MyImageMain {
			idc = 173;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage74: MyImageMain {
			idc = 174;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage75: MyImageMain {
			idc = 175;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage76: MyImageMain {
			idc = 176;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage77: MyImageMain {
			idc = 177;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage78: MyImageMain {
			idc = 178;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage79: MyImageMain {
			idc = 179;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage80: MyImageMain {
			idc = 180;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage81: MyImageMain {
			idc = 181;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage82: MyImageMain {
			idc = 182;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage83: MyImageMain {
			idc = 183;								    // id of control
			y = 2.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage84: MyImageMain {
			idc = 184;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage85: MyImageMain {
			idc = 185;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage86: MyImageMain {
			idc = 186;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage87: MyImageMain {
			idc = 187;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage88: MyImageMain {
			idc = 188;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage89: MyImageMain {
			idc = 189;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage90: MyImageMain {
			idc = 190;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage91: MyImageMain {
			idc = 191;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage92: MyImageMain {
			idc = 192;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage93: MyImageMain {
			idc = 193;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage94: MyImageMain {
			idc = 194;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage95: MyImageMain {
			idc = 195;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage96: MyImageMain {
			idc = 196;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage97: MyImageMain {
			idc = 197;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage98: MyImageMain {
			idc = 198;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage99: MyImageMain {
			idc = 199;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage100: MyImageMain {
			idc = 200;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage101: MyImageMain {
			idc = 201;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage102: MyImageMain {
			idc = 202;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage103: MyImageMain {
			idc = 203;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage104: MyImageMain {
			idc = 204;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage105: MyImageMain {
			idc = 205;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage106: MyImageMain {
			idc = 206;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage107: MyImageMain {
			idc = 207;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage108: MyImageMain {
			idc = 208;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage109: MyImageMain {
			idc = 209;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage110: MyImageMain {
			idc = 210;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage111: MyImageMain {
			idc = 211;								    // id of control
			y = 3.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage112: MyImageMain {
			idc = 212;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage113: MyImageMain {
			idc = 213;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage114: MyImageMain {
			idc = 214;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage115: MyImageMain {
			idc = 215;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage116: MyImageMain {
			idc = 216;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage117: MyImageMain {
			idc = 217;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage118: MyImageMain {
			idc = 218;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage119: MyImageMain {
			idc = 219;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage120: MyImageMain {
			idc = 220;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage121: MyImageMain {
			idc = 221;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage122: MyImageMain {
			idc = 222;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage123: MyImageMain {
			idc = 223;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage124: MyImageMain {
			idc = 224;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage125: MyImageMain {
			idc = 225;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage126: MyImageMain {
			idc = 226;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage127: MyImageMain {
			idc = 227;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage128: MyImageMain {
			idc = 228;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage129: MyImageMain {
			idc = 229;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage130: MyImageMain {
			idc = 230;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage131: MyImageMain {
			idc = 231;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage132: MyImageMain {
			idc = 232;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage133: MyImageMain {
			idc = 233;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage134: MyImageMain {
			idc = 234;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage135: MyImageMain {
			idc = 235;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage136: MyImageMain {
			idc = 236;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage137: MyImageMain {
			idc = 237;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage138: MyImageMain {
			idc = 238;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage139: MyImageMain {
			idc = 239;								    // id of control
			y = 4.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage140: MyImageMain {
			idc = 240;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage141: MyImageMain {
			idc = 241;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage142: MyImageMain {
			idc = 242;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage143: MyImageMain {
			idc = 243;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage144: MyImageMain {
			idc = 244;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage145: MyImageMain {
			idc = 245;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage146: MyImageMain {
			idc = 246;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage147: MyImageMain {
			idc = 247;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage148: MyImageMain {
			idc = 248;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage149: MyImageMain {
			idc = 249;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage150: MyImageMain {
			idc = 250;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage151: MyImageMain {
			idc = 251;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage152: MyImageMain {
			idc = 252;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage153: MyImageMain {
			idc = 253;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage154: MyImageMain {
			idc = 254;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage155: MyImageMain {
			idc = 255;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage156: MyImageMain {
			idc = 256;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage157: MyImageMain {
			idc = 257;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage158: MyImageMain {
			idc = 258;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage159: MyImageMain {
			idc = 259;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage160: MyImageMain {
			idc = 260;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage161: MyImageMain {
			idc = 261;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage162: MyImageMain {
			idc = 262;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage163: MyImageMain {
			idc = 263;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage164: MyImageMain {
			idc = 264;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage165: MyImageMain {
			idc = 265;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage166: MyImageMain {
			idc = 266;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage167: MyImageMain {
			idc = 267;								    // id of control
			y = 5.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage168: MyImageMain {
			idc = 268;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage169: MyImageMain {
			idc = 269;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage170: MyImageMain {
			idc = 270;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage171: MyImageMain {
			idc = 271;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage172: MyImageMain {
			idc = 272;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage173: MyImageMain {
			idc = 273;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage174: MyImageMain {
			idc = 274;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage175: MyImageMain {
			idc = 275;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage176: MyImageMain {
			idc = 276;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage177: MyImageMain {
			idc = 277;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage178: MyImageMain {
			idc = 278;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage179: MyImageMain {
			idc = 279;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage180: MyImageMain {
			idc = 280;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage181: MyImageMain {
			idc = 281;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage182: MyImageMain {
			idc = 282;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage183: MyImageMain {
			idc = 283;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage184: MyImageMain {
			idc = 284;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage185: MyImageMain {
			idc = 285;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage186: MyImageMain {
			idc = 286;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage187: MyImageMain {
			idc = 287;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage188: MyImageMain {
			idc = 288;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage189: MyImageMain {
			idc = 289;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage190: MyImageMain {
			idc = 290;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage191: MyImageMain {
			idc = 291;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage192: MyImageMain {
			idc = 292;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage193: MyImageMain {
			idc = 293;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage194: MyImageMain {
			idc = 294;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage195: MyImageMain {
			idc = 295;								    // id of control
			y = 6.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage196: MyImageMain {
			idc = 296;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage197: MyImageMain {
			idc = 297;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage198: MyImageMain {
			idc = 298;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage199: MyImageMain {
			idc = 299;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage200: MyImageMain {
			idc = 300;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage201: MyImageMain {
			idc = 301;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage202: MyImageMain {
			idc = 302;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage203: MyImageMain {
			idc = 303;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage204: MyImageMain {
			idc = 304;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage205: MyImageMain {
			idc = 305;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage206: MyImageMain {
			idc = 306;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage207: MyImageMain {
			idc = 307;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage208: MyImageMain {
			idc = 308;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage209: MyImageMain {
			idc = 309;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage210: MyImageMain {
			idc = 310;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage211: MyImageMain {
			idc = 311;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage212: MyImageMain {
			idc = 312;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage213: MyImageMain {
			idc = 313;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage214: MyImageMain {
			idc = 314;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage215: MyImageMain {
			idc = 315;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage216: MyImageMain {
			idc = 316;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage217: MyImageMain {
			idc = 317;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage218: MyImageMain {
			idc = 318;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage219: MyImageMain {
			idc = 319;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage220: MyImageMain {
			idc = 320;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage221: MyImageMain {
			idc = 321;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage222: MyImageMain {
			idc = 322;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage223: MyImageMain {
			idc = 323;								    // id of control
			y = 7.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage224: MyImageMain {
			idc = 324;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage225: MyImageMain {
			idc = 325;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage226: MyImageMain {
			idc = 326;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage227: MyImageMain {
			idc = 327;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage228: MyImageMain {
			idc = 328;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage229: MyImageMain {
			idc = 329;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage230: MyImageMain {
			idc = 330;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage231: MyImageMain {
			idc = 331;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage232: MyImageMain {
			idc = 332;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage233: MyImageMain {
			idc = 333;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage234: MyImageMain {
			idc = 334;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage235: MyImageMain {
			idc = 335;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage236: MyImageMain {
			idc = 336;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage237: MyImageMain {
			idc = 337;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage238: MyImageMain {
			idc = 338;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage239: MyImageMain {
			idc = 339;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage240: MyImageMain {
			idc = 340;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage241: MyImageMain {
			idc = 341;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage242: MyImageMain {
			idc = 342;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage243: MyImageMain {
			idc = 343;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage244: MyImageMain {
			idc = 344;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage245: MyImageMain {
			idc = 345;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage246: MyImageMain {
			idc = 346;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage247: MyImageMain {
			idc = 347;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage248: MyImageMain {
			idc = 348;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage249: MyImageMain {
			idc = 349;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage250: MyImageMain {
			idc = 350;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage251: MyImageMain {
			idc = 351;								    // id of control
			y = 8.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage252: MyImageMain {
			idc = 352;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage253: MyImageMain {
			idc = 353;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage254: MyImageMain {
			idc = 354;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage255: MyImageMain {
			idc = 355;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage256: MyImageMain {
			idc = 356;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage257: MyImageMain {
			idc = 357;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage258: MyImageMain {
			idc = 358;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage259: MyImageMain {
			idc = 359;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage260: MyImageMain {
			idc = 360;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage261: MyImageMain {
			idc = 361;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage262: MyImageMain {
			idc = 362;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage263: MyImageMain {
			idc = 363;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage264: MyImageMain {
			idc = 364;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage265: MyImageMain {
			idc = 365;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage266: MyImageMain {
			idc = 366;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage267: MyImageMain {
			idc = 367;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage268: MyImageMain {
			idc = 368;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage269: MyImageMain {
			idc = 369;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage270: MyImageMain {
			idc = 370;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage271: MyImageMain {
			idc = 371;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage272: MyImageMain {
			idc = 372;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage273: MyImageMain {
			idc = 373;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage274: MyImageMain {
			idc = 374;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage275: MyImageMain {
			idc = 375;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage276: MyImageMain {
			idc = 376;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage277: MyImageMain {
			idc = 377;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage278: MyImageMain {
			idc = 378;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage279: MyImageMain {
			idc = 379;								    // id of control
			y = 9.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage280: MyImageMain {
			idc = 380;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage281: MyImageMain {
			idc = 381;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage282: MyImageMain {
			idc = 382;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage283: MyImageMain {
			idc = 383;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage284: MyImageMain {
			idc = 384;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage285: MyImageMain {
			idc = 385;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage286: MyImageMain {
			idc = 386;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage287: MyImageMain {
			idc = 387;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage288: MyImageMain {
			idc = 388;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage289: MyImageMain {
			idc = 389;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage290: MyImageMain {
			idc = 390;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage291: MyImageMain {
			idc = 391;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage292: MyImageMain {
			idc = 392;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage293: MyImageMain {
			idc = 393;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage294: MyImageMain {
			idc = 394;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage295: MyImageMain {
			idc = 395;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage296: MyImageMain {
			idc = 396;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage297: MyImageMain {
			idc = 397;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage298: MyImageMain {
			idc = 398;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage299: MyImageMain {
			idc = 399;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage300: MyImageMain {
			idc = 400;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage301: MyImageMain {
			idc = 401;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage302: MyImageMain {
			idc = 402;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage303: MyImageMain {
			idc = 403;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage304: MyImageMain {
			idc = 404;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage305: MyImageMain {
			idc = 405;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage306: MyImageMain {
			idc = 406;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage307: MyImageMain {
			idc = 407;								    // id of control
			y = 10.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage308: MyImageMain {
			idc = 408;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage309: MyImageMain {
			idc = 409;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage310: MyImageMain {
			idc = 410;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage311: MyImageMain {
			idc = 411;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage312: MyImageMain {
			idc = 412;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage313: MyImageMain {
			idc = 413;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage314: MyImageMain {
			idc = 414;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage315: MyImageMain {
			idc = 415;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage316: MyImageMain {
			idc = 416;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage317: MyImageMain {
			idc = 417;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage318: MyImageMain {
			idc = 418;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage319: MyImageMain {
			idc = 419;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage320: MyImageMain {
			idc = 420;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage321: MyImageMain {
			idc = 421;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage322: MyImageMain {
			idc = 422;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage323: MyImageMain {
			idc = 423;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage324: MyImageMain {
			idc = 424;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage325: MyImageMain {
			idc = 425;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage326: MyImageMain {
			idc = 426;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage327: MyImageMain {
			idc = 427;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage328: MyImageMain {
			idc = 428;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage329: MyImageMain {
			idc = 429;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage330: MyImageMain {
			idc = 430;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage331: MyImageMain {
			idc = 431;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage332: MyImageMain {
			idc = 432;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage333: MyImageMain {
			idc = 433;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage334: MyImageMain {
			idc = 434;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage335: MyImageMain {
			idc = 435;								    // id of control
			y = 11.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage336: MyImageMain {
			idc = 436;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage337: MyImageMain {
			idc = 437;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage338: MyImageMain {
			idc = 438;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage339: MyImageMain {
			idc = 439;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage340: MyImageMain {
			idc = 440;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage341: MyImageMain {
			idc = 441;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage342: MyImageMain {
			idc = 442;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage343: MyImageMain {
			idc = 443;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage344: MyImageMain {
			idc = 444;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage345: MyImageMain {
			idc = 445;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage346: MyImageMain {
			idc = 446;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage347: MyImageMain {
			idc = 447;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage348: MyImageMain {
			idc = 448;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage349: MyImageMain {
			idc = 449;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage350: MyImageMain {
			idc = 450;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage351: MyImageMain {
			idc = 451;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage352: MyImageMain {
			idc = 452;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage353: MyImageMain {
			idc = 453;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage354: MyImageMain {
			idc = 454;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage355: MyImageMain {
			idc = 455;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage356: MyImageMain {
			idc = 456;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage357: MyImageMain {
			idc = 457;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage358: MyImageMain {
			idc = 458;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage359: MyImageMain {
			idc = 459;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage360: MyImageMain {
			idc = 460;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage361: MyImageMain {
			idc = 461;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage362: MyImageMain {
			idc = 462;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage363: MyImageMain {
			idc = 463;								    // id of control
			y = 12.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage364: MyImageMain {
			idc = 464;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage365: MyImageMain {
			idc = 465;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage366: MyImageMain {
			idc = 466;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage367: MyImageMain {
			idc = 467;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage368: MyImageMain {
			idc = 468;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage369: MyImageMain {
			idc = 469;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage370: MyImageMain {
			idc = 470;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage371: MyImageMain {
			idc = 471;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage372: MyImageMain {
			idc = 472;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage373: MyImageMain {
			idc = 473;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage374: MyImageMain {
			idc = 474;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage375: MyImageMain {
			idc = 475;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage376: MyImageMain {
			idc = 476;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage377: MyImageMain {
			idc = 477;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage378: MyImageMain {
			idc = 478;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage379: MyImageMain {
			idc = 479;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage380: MyImageMain {
			idc = 480;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage381: MyImageMain {
			idc = 481;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage382: MyImageMain {
			idc = 482;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage383: MyImageMain {
			idc = 483;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage384: MyImageMain {
			idc = 484;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage385: MyImageMain {
			idc = 485;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage386: MyImageMain {
			idc = 486;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage387: MyImageMain {
			idc = 487;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage388: MyImageMain {
			idc = 488;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage389: MyImageMain {
			idc = 489;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage390: MyImageMain {
			idc = 490;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage391: MyImageMain {
			idc = 491;								    // id of control
			y = 13.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage392: MyImageMain {
			idc = 492;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage393: MyImageMain {
			idc = 493;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage394: MyImageMain {
			idc = 494;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage395: MyImageMain {
			idc = 495;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage396: MyImageMain {
			idc = 496;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage397: MyImageMain {
			idc = 497;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage398: MyImageMain {
			idc = 498;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage399: MyImageMain {
			idc = 499;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage400: MyImageMain {
			idc = 500;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage401: MyImageMain {
			idc = 501;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage402: MyImageMain {
			idc = 502;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage403: MyImageMain {
			idc = 503;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage404: MyImageMain {
			idc = 504;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage405: MyImageMain {
			idc = 505;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage406: MyImageMain {
			idc = 506;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage407: MyImageMain {
			idc = 507;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage408: MyImageMain {
			idc = 508;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage409: MyImageMain {
			idc = 509;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage410: MyImageMain {
			idc = 510;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage411: MyImageMain {
			idc = 511;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage412: MyImageMain {
			idc = 512;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage413: MyImageMain {
			idc = 513;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage414: MyImageMain {
			idc = 514;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage415: MyImageMain {
			idc = 515;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage416: MyImageMain {
			idc = 516;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage417: MyImageMain {
			idc = 517;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage418: MyImageMain {
			idc = 518;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage419: MyImageMain {
			idc = 519;								    // id of control
			y = 14.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage420: MyImageMain {
			idc = 520;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage421: MyImageMain {
			idc = 521;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage422: MyImageMain {
			idc = 522;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage423: MyImageMain {
			idc = 523;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage424: MyImageMain {
			idc = 524;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage425: MyImageMain {
			idc = 525;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage426: MyImageMain {
			idc = 526;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage427: MyImageMain {
			idc = 527;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage428: MyImageMain {
			idc = 528;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage429: MyImageMain {
			idc = 529;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage430: MyImageMain {
			idc = 530;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage431: MyImageMain {
			idc = 531;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage432: MyImageMain {
			idc = 532;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage433: MyImageMain {
			idc = 533;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage434: MyImageMain {
			idc = 534;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage435: MyImageMain {
			idc = 535;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage436: MyImageMain {
			idc = 536;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage437: MyImageMain {
			idc = 537;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage438: MyImageMain {
			idc = 538;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage439: MyImageMain {
			idc = 539;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage440: MyImageMain {
			idc = 540;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage441: MyImageMain {
			idc = 541;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage442: MyImageMain {
			idc = 542;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage443: MyImageMain {
			idc = 543;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage444: MyImageMain {
			idc = 544;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage445: MyImageMain {
			idc = 545;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage446: MyImageMain {
			idc = 546;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage447: MyImageMain {
			idc = 547;								    // id of control
			y = 15.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage448: MyImageMain {
			idc = 548;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage449: MyImageMain {
			idc = 549;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage450: MyImageMain {
			idc = 550;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage451: MyImageMain {
			idc = 551;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage452: MyImageMain {
			idc = 552;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage453: MyImageMain {
			idc = 553;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage454: MyImageMain {
			idc = 554;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage455: MyImageMain {
			idc = 555;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage456: MyImageMain {
			idc = 556;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage457: MyImageMain {
			idc = 557;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage458: MyImageMain {
			idc = 558;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage459: MyImageMain {
			idc = 559;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage460: MyImageMain {
			idc = 560;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage461: MyImageMain {
			idc = 561;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage462: MyImageMain {
			idc = 562;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage463: MyImageMain {
			idc = 563;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage464: MyImageMain {
			idc = 564;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage465: MyImageMain {
			idc = 565;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage466: MyImageMain {
			idc = 566;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage467: MyImageMain {
			idc = 567;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage468: MyImageMain {
			idc = 568;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage469: MyImageMain {
			idc = 569;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage470: MyImageMain {
			idc = 570;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage471: MyImageMain {
			idc = 571;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage472: MyImageMain {
			idc = 572;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage473: MyImageMain {
			idc = 573;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage474: MyImageMain {
			idc = 574;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage475: MyImageMain {
			idc = 575;								    // id of control
			y = 16.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage476: MyImageMain {
			idc = 576;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage477: MyImageMain {
			idc = 577;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage478: MyImageMain {
			idc = 578;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage479: MyImageMain {
			idc = 579;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage480: MyImageMain {
			idc = 580;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage481: MyImageMain {
			idc = 581;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage482: MyImageMain {
			idc = 582;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage483: MyImageMain {
			idc = 583;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage484: MyImageMain {
			idc = 584;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage485: MyImageMain {
			idc = 585;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage486: MyImageMain {
			idc = 586;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage487: MyImageMain {
			idc = 587;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage488: MyImageMain {
			idc = 588;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage489: MyImageMain {
			idc = 589;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage490: MyImageMain {
			idc = 590;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage491: MyImageMain {
			idc = 591;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage492: MyImageMain {
			idc = 592;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage493: MyImageMain {
			idc = 593;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage494: MyImageMain {
			idc = 594;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage495: MyImageMain {
			idc = 595;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage496: MyImageMain {
			idc = 596;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage497: MyImageMain {
			idc = 597;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage498: MyImageMain {
			idc = 598;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage499: MyImageMain {
			idc = 599;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage500: MyImageMain {
			idc = 600;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage501: MyImageMain {
			idc = 601;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage502: MyImageMain {
			idc = 602;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage503: MyImageMain {
			idc = 603;								    // id of control
			y = 17.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage504: MyImageMain {
			idc = 604;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage505: MyImageMain {
			idc = 605;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage506: MyImageMain {
			idc = 606;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage507: MyImageMain {
			idc = 607;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage508: MyImageMain {
			idc = 608;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage509: MyImageMain {
			idc = 609;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage510: MyImageMain {
			idc = 610;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage511: MyImageMain {
			idc = 611;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage512: MyImageMain {
			idc = 612;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage513: MyImageMain {
			idc = 613;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage514: MyImageMain {
			idc = 614;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage515: MyImageMain {
			idc = 615;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage516: MyImageMain {
			idc = 616;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage517: MyImageMain {
			idc = 617;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage518: MyImageMain {
			idc = 618;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage519: MyImageMain {
			idc = 619;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage520: MyImageMain {
			idc = 620;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage521: MyImageMain {
			idc = 621;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage522: MyImageMain {
			idc = 622;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage523: MyImageMain {
			idc = 623;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage524: MyImageMain {
			idc = 624;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage525: MyImageMain {
			idc = 625;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage526: MyImageMain {
			idc = 626;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage527: MyImageMain {
			idc = 627;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage528: MyImageMain {
			idc = 628;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage529: MyImageMain {
			idc = 629;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage530: MyImageMain {
			idc = 630;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage531: MyImageMain {
			idc = 631;								    // id of control
			y = 18.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage532: MyImageMain {
			idc = 632;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage533: MyImageMain {
			idc = 633;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage534: MyImageMain {
			idc = 634;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage535: MyImageMain {
			idc = 635;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage536: MyImageMain {
			idc = 636;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage537: MyImageMain {
			idc = 637;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage538: MyImageMain {
			idc = 638;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage539: MyImageMain {
			idc = 639;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage540: MyImageMain {
			idc = 640;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage541: MyImageMain {
			idc = 641;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage542: MyImageMain {
			idc = 642;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage543: MyImageMain {
			idc = 643;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage544: MyImageMain {
			idc = 644;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage545: MyImageMain {
			idc = 645;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage546: MyImageMain {
			idc = 646;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage547: MyImageMain {
			idc = 647;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage548: MyImageMain {
			idc = 648;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage549: MyImageMain {
			idc = 649;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage550: MyImageMain {
			idc = 650;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage551: MyImageMain {
			idc = 651;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage552: MyImageMain {
			idc = 652;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage553: MyImageMain {
			idc = 653;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage554: MyImageMain {
			idc = 654;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage555: MyImageMain {
			idc = 655;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage556: MyImageMain {
			idc = 656;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage557: MyImageMain {
			idc = 657;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage558: MyImageMain {
			idc = 658;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage559: MyImageMain {
			idc = 659;								    // id of control
			y = 19.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage560: MyImageMain {
			idc = 660;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage561: MyImageMain {
			idc = 661;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage562: MyImageMain {
			idc = 662;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage563: MyImageMain {
			idc = 663;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage564: MyImageMain {
			idc = 664;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage565: MyImageMain {
			idc = 665;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage566: MyImageMain {
			idc = 666;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage567: MyImageMain {
			idc = 667;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage568: MyImageMain {
			idc = 668;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage569: MyImageMain {
			idc = 669;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage570: MyImageMain {
			idc = 670;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage571: MyImageMain {
			idc = 671;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage572: MyImageMain {
			idc = 672;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage573: MyImageMain {
			idc = 673;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage574: MyImageMain {
			idc = 674;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage575: MyImageMain {
			idc = 675;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage576: MyImageMain {
			idc = 676;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage577: MyImageMain {
			idc = 677;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage578: MyImageMain {
			idc = 678;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage579: MyImageMain {
			idc = 679;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage580: MyImageMain {
			idc = 680;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage581: MyImageMain {
			idc = 681;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage582: MyImageMain {
			idc = 682;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage583: MyImageMain {
			idc = 683;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage584: MyImageMain {
			idc = 684;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage585: MyImageMain {
			idc = 685;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage586: MyImageMain {
			idc = 686;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage587: MyImageMain {
			idc = 687;								    // id of control
			y = 20.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage588: MyImageMain {
			idc = 688;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage589: MyImageMain {
			idc = 689;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage590: MyImageMain {
			idc = 690;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage591: MyImageMain {
			idc = 691;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage592: MyImageMain {
			idc = 692;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage593: MyImageMain {
			idc = 693;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage594: MyImageMain {
			idc = 694;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage595: MyImageMain {
			idc = 695;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage596: MyImageMain {
			idc = 696;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage597: MyImageMain {
			idc = 697;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage598: MyImageMain {
			idc = 698;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage599: MyImageMain {
			idc = 699;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage600: MyImageMain {
			idc = 700;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage601: MyImageMain {
			idc = 701;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage602: MyImageMain {
			idc = 702;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage603: MyImageMain {
			idc = 703;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage604: MyImageMain {
			idc = 704;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage605: MyImageMain {
			idc = 705;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage606: MyImageMain {
			idc = 706;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage607: MyImageMain {
			idc = 707;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage608: MyImageMain {
			idc = 708;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage609: MyImageMain {
			idc = 709;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage610: MyImageMain {
			idc = 710;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage611: MyImageMain {
			idc = 711;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage612: MyImageMain {
			idc = 712;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage613: MyImageMain {
			idc = 713;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage614: MyImageMain {
			idc = 714;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage615: MyImageMain {
			idc = 715;								    // id of control
			y = 21.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage616: MyImageMain {
			idc = 716;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage617: MyImageMain {
			idc = 717;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage618: MyImageMain {
			idc = 718;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage619: MyImageMain {
			idc = 719;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage620: MyImageMain {
			idc = 720;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage621: MyImageMain {
			idc = 721;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage622: MyImageMain {
			idc = 722;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage623: MyImageMain {
			idc = 723;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage624: MyImageMain {
			idc = 724;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage625: MyImageMain {
			idc = 725;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage626: MyImageMain {
			idc = 726;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage627: MyImageMain {
			idc = 727;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage628: MyImageMain {
			idc = 728;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage629: MyImageMain {
			idc = 729;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage630: MyImageMain {
			idc = 730;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage631: MyImageMain {
			idc = 731;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage632: MyImageMain {
			idc = 732;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage633: MyImageMain {
			idc = 733;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage634: MyImageMain {
			idc = 734;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage635: MyImageMain {
			idc = 735;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage636: MyImageMain {
			idc = 736;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage637: MyImageMain {
			idc = 737;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage638: MyImageMain {
			idc = 738;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage639: MyImageMain {
			idc = 739;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage640: MyImageMain {
			idc = 740;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage641: MyImageMain {
			idc = 741;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage642: MyImageMain {
			idc = 742;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage643: MyImageMain {
			idc = 743;								    // id of control
			y = 22.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage644: MyImageMain {
			idc = 744;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage645: MyImageMain {
			idc = 745;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage646: MyImageMain {
			idc = 746;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage647: MyImageMain {
			idc = 747;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage648: MyImageMain {
			idc = 748;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage649: MyImageMain {
			idc = 749;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage650: MyImageMain {
			idc = 750;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage651: MyImageMain {
			idc = 751;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage652: MyImageMain {
			idc = 752;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage653: MyImageMain {
			idc = 753;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage654: MyImageMain {
			idc = 754;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage655: MyImageMain {
			idc = 755;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage656: MyImageMain {
			idc = 756;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage657: MyImageMain {
			idc = 757;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage658: MyImageMain {
			idc = 758;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage659: MyImageMain {
			idc = 759;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage660: MyImageMain {
			idc = 760;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage661: MyImageMain {
			idc = 761;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage662: MyImageMain {
			idc = 762;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage663: MyImageMain {
			idc = 763;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage664: MyImageMain {
			idc = 764;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage665: MyImageMain {
			idc = 765;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage666: MyImageMain {
			idc = 766;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage667: MyImageMain {
			idc = 767;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage668: MyImageMain {
			idc = 768;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage669: MyImageMain {
			idc = 769;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage670: MyImageMain {
			idc = 770;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage671: MyImageMain {
			idc = 771;								    // id of control
			y = 23.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage672: MyImageMain {
			idc = 772;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage673: MyImageMain {
			idc = 773;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage674: MyImageMain {
			idc = 774;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage675: MyImageMain {
			idc = 775;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage676: MyImageMain {
			idc = 776;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage677: MyImageMain {
			idc = 777;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage678: MyImageMain {
			idc = 778;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage679: MyImageMain {
			idc = 779;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage680: MyImageMain {
			idc = 780;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage681: MyImageMain {
			idc = 781;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage682: MyImageMain {
			idc = 782;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage683: MyImageMain {
			idc = 783;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage684: MyImageMain {
			idc = 784;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage685: MyImageMain {
			idc = 785;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage686: MyImageMain {
			idc = 786;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage687: MyImageMain {
			idc = 787;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage688: MyImageMain {
			idc = 788;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage689: MyImageMain {
			idc = 789;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage690: MyImageMain {
			idc = 790;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage691: MyImageMain {
			idc = 791;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage692: MyImageMain {
			idc = 792;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage693: MyImageMain {
			idc = 793;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage694: MyImageMain {
			idc = 794;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage695: MyImageMain {
			idc = 795;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage696: MyImageMain {
			idc = 796;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage697: MyImageMain {
			idc = 797;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage698: MyImageMain {
			idc = 798;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage699: MyImageMain {
			idc = 799;								    // id of control
			y = 24.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage700: MyImageMain {
			idc = 800;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage701: MyImageMain {
			idc = 801;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage702: MyImageMain {
			idc = 802;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage703: MyImageMain {
			idc = 803;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage704: MyImageMain {
			idc = 804;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage705: MyImageMain {
			idc = 805;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage706: MyImageMain {
			idc = 806;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage707: MyImageMain {
			idc = 807;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage708: MyImageMain {
			idc = 808;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage709: MyImageMain {
			idc = 809;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage710: MyImageMain {
			idc = 810;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage711: MyImageMain {
			idc = 811;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage712: MyImageMain {
			idc = 812;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage713: MyImageMain {
			idc = 813;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage714: MyImageMain {
			idc = 814;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage715: MyImageMain {
			idc = 815;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage716: MyImageMain {
			idc = 816;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage717: MyImageMain {
			idc = 817;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage718: MyImageMain {
			idc = 818;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage719: MyImageMain {
			idc = 819;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage720: MyImageMain {
			idc = 820;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage721: MyImageMain {
			idc = 821;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage722: MyImageMain {
			idc = 822;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage723: MyImageMain {
			idc = 823;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage724: MyImageMain {
			idc = 824;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage725: MyImageMain {
			idc = 825;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage726: MyImageMain {
			idc = 826;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage727: MyImageMain {
			idc = 827;								    // id of control
			y = 25.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage728: MyImageMain {
			idc = 828;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage729: MyImageMain {
			idc = 829;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage730: MyImageMain {
			idc = 830;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage731: MyImageMain {
			idc = 831;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage732: MyImageMain {
			idc = 832;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage733: MyImageMain {
			idc = 833;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage734: MyImageMain {
			idc = 834;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage735: MyImageMain {
			idc = 835;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage736: MyImageMain {
			idc = 836;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage737: MyImageMain {
			idc = 837;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage738: MyImageMain {
			idc = 838;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage739: MyImageMain {
			idc = 839;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage740: MyImageMain {
			idc = 840;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage741: MyImageMain {
			idc = 841;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage742: MyImageMain {
			idc = 842;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage743: MyImageMain {
			idc = 843;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage744: MyImageMain {
			idc = 844;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage745: MyImageMain {
			idc = 845;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage746: MyImageMain {
			idc = 846;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage747: MyImageMain {
			idc = 847;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage748: MyImageMain {
			idc = 848;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage749: MyImageMain {
			idc = 849;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage750: MyImageMain {
			idc = 850;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage751: MyImageMain {
			idc = 851;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage752: MyImageMain {
			idc = 852;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage753: MyImageMain {
			idc = 853;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage754: MyImageMain {
			idc = 854;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage755: MyImageMain {
			idc = 855;								    // id of control
			y = 26.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage756: MyImageMain {
			idc = 856;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage757: MyImageMain {
			idc = 857;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage758: MyImageMain {
			idc = 858;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage759: MyImageMain {
			idc = 859;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage760: MyImageMain {
			idc = 860;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage761: MyImageMain {
			idc = 861;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage762: MyImageMain {
			idc = 862;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage763: MyImageMain {
			idc = 863;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage764: MyImageMain {
			idc = 864;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage765: MyImageMain {
			idc = 865;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage766: MyImageMain {
			idc = 866;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage767: MyImageMain {
			idc = 867;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage768: MyImageMain {
			idc = 868;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage769: MyImageMain {
			idc = 869;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage770: MyImageMain {
			idc = 870;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage771: MyImageMain {
			idc = 871;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage772: MyImageMain {
			idc = 872;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage773: MyImageMain {
			idc = 873;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage774: MyImageMain {
			idc = 874;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage775: MyImageMain {
			idc = 875;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage776: MyImageMain {
			idc = 876;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage777: MyImageMain {
			idc = 877;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage778: MyImageMain {
			idc = 878;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage779: MyImageMain {
			idc = 879;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage780: MyImageMain {
			idc = 880;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage781: MyImageMain {
			idc = 881;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage782: MyImageMain {
			idc = 882;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage783: MyImageMain {
			idc = 883;								    // id of control
			y = 27.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage784: MyImageMain {
			idc = 884;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage785: MyImageMain {
			idc = 885;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage786: MyImageMain {
			idc = 886;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage787: MyImageMain {
			idc = 887;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage788: MyImageMain {
			idc = 888;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage789: MyImageMain {
			idc = 889;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage790: MyImageMain {
			idc = 890;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage791: MyImageMain {
			idc = 891;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage792: MyImageMain {
			idc = 892;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage793: MyImageMain {
			idc = 893;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage794: MyImageMain {
			idc = 894;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage795: MyImageMain {
			idc = 895;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage796: MyImageMain {
			idc = 896;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage797: MyImageMain {
			idc = 897;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage798: MyImageMain {
			idc = 898;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage799: MyImageMain {
			idc = 899;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage800: MyImageMain {
			idc = 900;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage801: MyImageMain {
			idc = 901;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage802: MyImageMain {
			idc = 902;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage803: MyImageMain {
			idc = 903;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage804: MyImageMain {
			idc = 904;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage805: MyImageMain {
			idc = 905;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage806: MyImageMain {
			idc = 906;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage807: MyImageMain {
			idc = 907;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage808: MyImageMain {
			idc = 908;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage809: MyImageMain {
			idc = 909;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage810: MyImageMain {
			idc = 910;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage811: MyImageMain {
			idc = 911;								    // id of control
			y = 28.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage812: MyImageMain {
			idc = 912;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage813: MyImageMain {
			idc = 913;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage814: MyImageMain {
			idc = 914;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage815: MyImageMain {
			idc = 915;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage816: MyImageMain {
			idc = 916;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage817: MyImageMain {
			idc = 917;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage818: MyImageMain {
			idc = 918;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage819: MyImageMain {
			idc = 919;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage820: MyImageMain {
			idc = 920;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage821: MyImageMain {
			idc = 921;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage822: MyImageMain {
			idc = 922;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage823: MyImageMain {
			idc = 923;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage824: MyImageMain {
			idc = 924;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage825: MyImageMain {
			idc = 925;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage826: MyImageMain {
			idc = 926;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage827: MyImageMain {
			idc = 927;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage828: MyImageMain {
			idc = 928;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage829: MyImageMain {
			idc = 929;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage830: MyImageMain {
			idc = 930;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage831: MyImageMain {
			idc = 931;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage832: MyImageMain {
			idc = 932;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage833: MyImageMain {
			idc = 933;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage834: MyImageMain {
			idc = 934;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage835: MyImageMain {
			idc = 935;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage836: MyImageMain {
			idc = 936;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage837: MyImageMain {
			idc = 937;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage838: MyImageMain {
			idc = 938;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage839: MyImageMain {
			idc = 939;								    // id of control
			y = 29.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage840: MyImageMain {
			idc = 940;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage841: MyImageMain {
			idc = 941;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage842: MyImageMain {
			idc = 942;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage843: MyImageMain {
			idc = 943;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage844: MyImageMain {
			idc = 944;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage845: MyImageMain {
			idc = 945;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage846: MyImageMain {
			idc = 946;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage847: MyImageMain {
			idc = 947;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage848: MyImageMain {
			idc = 948;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage849: MyImageMain {
			idc = 949;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage850: MyImageMain {
			idc = 950;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage851: MyImageMain {
			idc = 951;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage852: MyImageMain {
			idc = 952;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage853: MyImageMain {
			idc = 953;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage854: MyImageMain {
			idc = 954;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage855: MyImageMain {
			idc = 955;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage856: MyImageMain {
			idc = 956;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage857: MyImageMain {
			idc = 957;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage858: MyImageMain {
			idc = 958;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage859: MyImageMain {
			idc = 959;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage860: MyImageMain {
			idc = 960;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage861: MyImageMain {
			idc = 961;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage862: MyImageMain {
			idc = 962;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage863: MyImageMain {
			idc = 963;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage864: MyImageMain {
			idc = 964;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage865: MyImageMain {
			idc = 965;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage866: MyImageMain {
			idc = 966;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage867: MyImageMain {
			idc = 967;								    // id of control
			y = 30.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage868: MyImageMain {
			idc = 968;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 0.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage869: MyImageMain {
			idc = 969;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 1.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage870: MyImageMain {
			idc = 970;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 2.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage871: MyImageMain {
			idc = 971;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 3.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage872: MyImageMain {
			idc = 972;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 4.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage873: MyImageMain {
			idc = 973;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 5.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage874: MyImageMain {
			idc = 974;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 6.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage875: MyImageMain {
			idc = 975;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 7.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage876: MyImageMain {
			idc = 976;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 8.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage877: MyImageMain {
			idc = 977;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 9.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage878: MyImageMain {
			idc = 978;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 10.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage879: MyImageMain {
			idc = 979;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 11.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage880: MyImageMain {
			idc = 980;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 12.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage881: MyImageMain {
			idc = 981;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 13.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage882: MyImageMain {
			idc = 982;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 14.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage883: MyImageMain {
			idc = 983;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 15.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage884: MyImageMain {
			idc = 984;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 16.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage885: MyImageMain {
			idc = 985;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 17.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage886: MyImageMain {
			idc = 986;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 18.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage887: MyImageMain {
			idc = 987;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 19.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage888: MyImageMain {
			idc = 988;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 20.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage889: MyImageMain {
			idc = 989;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 21.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage890: MyImageMain {
			idc = 990;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 22.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage891: MyImageMain {
			idc = 991;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 23.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage892: MyImageMain {
			idc = 992;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 24.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage893: MyImageMain {
			idc = 993;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 25.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage894: MyImageMain {
			idc = 994;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 26.0 * cell_w + cell_first_x;           // image position horizontal
        };

        class MyImage895: MyImageMain {
			idc = 995;								    // id of control
			y = 31.0 * cell_h + cell_first_y;			// image position vertical
			x = 27.0 * cell_w + cell_first_x;           // image position horizontal
        };

    };
}; 

class CfgSounds {
	sounds[] = {};   
	class chomp {
		name = "";
		sound[] = {"\eatfruit.ogg", 5, 2};
		titles[] = {};
	};
	class death {
		name = "";
		sound[] = {"\death.ogg", 5, 1};
		titles[] = {};
	};
	class beginning {
		name = "";
		sound[] = {"\beginning.ogg", 5, 1};
		titles[] = {};
	};
};
